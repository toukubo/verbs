interpreter = {}
interpreter._interpret = function(hay,noun){
   // const hay = hay.replace(/noun/g, 'references')
   // console.log(hay) // apple banana orange
      
      return hay     
}
module.exports =  interpreter
